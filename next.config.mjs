
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'elated-wren-402.convex.cloud',
            }
        ]
    }
};

export default nextConfig;
